# infrastructure/ansible/templates/sources.list.j2
# Tuna Mirror Configuration for Debian
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

# Main repository
deb {{ tuna_mirror_url }}/debian/ {{ debian_version }} main contrib non-free non-free-firmware
deb-src {{ tuna_mirror_url }}/debian/ {{ debian_version }} main contrib non-free non-free-firmware

# Security updates
deb {{ tuna_mirror_url }}/debian-security {{ debian_version }}-security main contrib non-free non-free-firmware
deb-src {{ tuna_mirror_url }}/debian-security {{ debian_version }}-security main contrib non-free non-free-firmware

# Updates
deb {{ tuna_mirror_url }}/debian/ {{ debian_version }}-updates main contrib non-free non-free-firmware
deb-src {{ tuna_mirror_url }}/debian/ {{ debian_version }}-updates main contrib non-free non-free-firmware

# Backports (optional)
deb {{ tuna_mirror_url }}/debian/ {{ debian_version }}-backports main contrib non-free non-free-firmware
deb-src {{ tuna_mirror_url }}/debian/ {{ debian_version }}-backports main contrib non-free non-free-firmware